this toolbox store some repair tool like:
iam forward to set params shortest and need filter to easy maintain
filter tool(foptimm: f-filter, opti-optimusrobot-opti branch, mm:optimize/optimaze):
+)tool make name params short (BE-many,FE-less)
+)tool make name params detail easy read
+)tool xoa ten duoi file
+) tool chinh du lieu bit cho may tinh xu ly
+) tool tìm chuoi string array rỗng chuyển về null hết // hay là để chuỗi string trống hơn null vì nếu không để null nó sẽ khởi tạo o nhớ
+) tool optimize the link import to project standard like './../the-link' if optimize '../the-link'
