this forlder use for lego
this forlder merge many files to one file(tranformer-AOOA(ALLONE) - 1) - if CPU strong because it need search
and split/separeate/deatched one file to many files (detach-OAAO(ONEALL) - manyname - 2) - if MEMORY Strong even use resource
